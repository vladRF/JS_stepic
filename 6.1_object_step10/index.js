let place = "Магадан";
let date = "12.12.2023";
let durationDays = "неопределенное количество";

let trip = {
    place,
    date,
    durationDays,
};

console.log(`${trip.date} вы отправляетесь в ${trip.place} на ${trip.durationDays} дней`)